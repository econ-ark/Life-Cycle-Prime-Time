#!/bin/bash

# Install the conda environment:
# - $(sudo mamba env create -f myst.yml --name mystery)

# Activate it: $(conda activate mystery)

# Install node.js version 18 or higher for myst
# - confirm that npm is available at the command line: $(which npm)
# - if the path is not inside the system [ana/mini]conda directory, 
#   - if not: $(brew uninstall --force node; brew install node)

# then install myst:
# $ npm install -g mystmd

# Run it from the command line in a directory containing a markdown file:
# $ myst start



